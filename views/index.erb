<html>
    <body>
        <center>
            <h2> Enter the URL that you want to shorten! </h2>
            <h3>(Right click and open in new tab or copy and paste the shortened link)</h3>
            <form action = '/create_shortened_url' method ='post'>
                <input type = 'text' name ='original_url'>
                <input type = 'submit' value = 'shorten!'>
            </form>

        </center>
            <ul>
                <% Url.links.reverse_each do |shortened_url, original_url| %>
                    <li> 
                        Use <a href="localhost:4567/<%= shortened_url%>"> localhost:4567/<%= shortened_url%> </a> instead of
                         <a href="<%= original_url%>"> <%= original_url%> </a>
                    </li>
                <% end %>
            </ul> 
        
    </body>
</html>